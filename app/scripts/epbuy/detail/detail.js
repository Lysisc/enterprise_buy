'use strict';

angular.module('EPBUY')
    .controller('DetailCtrl', function ($scope, $stateParams, $ionicActionSheet, $userService, $state, $compile, ENV, Util, $location) {

        $scope.titleName = '我是商品名称';

        // alert(1);

    });
